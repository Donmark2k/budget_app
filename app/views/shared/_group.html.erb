<div class="group_card">
<div class="group_card_image">
    
<%= image_tag(group.icon, alt: 'Image Description') %>

</div>
 <div class="group_card_detail">
 <div>
<%# <h2><%= link_to group.name, group.user %></h2> %>
<h2><%= link_to group.name, user_group_path(group.user, group) %></h2>

<%# <p> $<%= group.expenses.sum(:amount) %></p> %>
</div>
<p> <%= group.created_at.strftime("%d %b %Y") %></p>

</div>
</div>
