
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
<nav class="">
  <div class="navbar">
 <p>
  <span>
    <%= link_to '←', splash_path, class: 'arrow-link' %>
  </span>
</p>
    <h2 class="login">FORGOT PASSWORD</h2>
    <div class="actions">
    <%= f.submit "Log in", class: 'login-btn' %>
  </div>
  </div>
</nav>
  <%= render "devise/shared/error_messages", resource: resource %>
<h3>Forgot your password?</h3>

  <div class="field">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Enter your mail", class: 'login-field' %>
  </div>

  <div class="actions">
    <%= f.submit "Send me reset password instructions", class: 'category_button'%>
  </div>
<% end %>

<% render "devise/shared/links" %>
