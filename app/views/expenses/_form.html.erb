<%= render partial: 'shared/nav', locals: {title:'TRANSACTION', symbol:'←', url: user_groups_path}%>
<div class="category_form">
<%= form_with(model: expense, url: user_group_expenses_path) do |form| %>
  <div class="transaction">
    <h3 class="">Add Expenses</h3>
  

  <div class="form_div">
    <%= form.label :name, class: 'name_label' %>
    <%= form.text_field :name, class:'' %>
  </div><br><br>

  <div class="form_div">
    <%= form.label :amount, class: '' %>
    <%= form.text_field :amount, class:'' %>
  </div><br><br>

  <div class="form_div">
    <label class="name_label">Select a category</label>
    <%= form.select :group_id, @group.all.collect {|u| [u.name.capitalize, u.id] }, class:'' %>
  </div>

  <div class=''>
    <%= form.submit "Add transaction", class:"category_button" %>
  </div>
  </div>
<% end %>
</div>