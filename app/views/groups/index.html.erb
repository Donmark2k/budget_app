<section class="">
  <p> <%= notice %></p>
  <p> Total expenses for <%= current_user.name.titleize %> : $<%=@total_expenses %> </p>
  
  <div>
  <% @groups.each do |group| %>
  <%= render partial: 'shared/group', locals: { group: group } %>
<%end%>
      </div>
      <%= button_to 'ADD CATEGORY', new_user_group_path(user_id: current_user.id), method: :get, class: ''%>
</section>