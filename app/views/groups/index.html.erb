<%= render partial: 'shared/nav', locals: {title:'CATEGORIES', symbol:'', url: ''}%>

<section class="body_container">
  <p> <%= notice %></p>
  <div  class="total_box"> 
  <p class="group_name"> Total expenses for <%= current_user.name.titleize %> </p>
   <p class="total_amount"> $<%=@total_expenses %> </p>
  </div>
  <div>
  <% @groups.includes(:expenses).each do |group| %>
  <%= render partial: 'shared/group', locals: { group: group } %>
<%end%>
      </div>
      <div class="button_box">
      <div class="push_box">
      <%= button_to 'ADD CATEGORY', new_user_group_path(user_id: current_user.id), method: :get, class: 'add_category fixed-bottom'%> </div>
      </div>
</section>